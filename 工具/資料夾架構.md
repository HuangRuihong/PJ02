以下是目前 `c:\PJ02\mysalf` 的完整資料夾架構：

```text
mysalf/
├── .gitignore                            # Git 忽略規則
├── backend                               # 後端業務邏輯與資料層
│   ├── core                             # 核心邏輯目錄，負責所有資料處理與邏輯運算
│   │   ├── base.py                     # 基礎服務類別，負責資料庫連線初始化與共用設定
│   │   ├── group_service.py            # 群組記帳與邏輯服務 (如網狀/星狀分帳)
│   │   ├── main.py                     # 系統核心邏輯對外入口 (Facade 模式整合)
│   │   ├── models.py                   # 各資料模型 (Models) 與狀態列舉 (Enum)
│   │   └── personal_service.py         # 個人記帳、好友關係與 QR Code 處理
│   └── data                             # 系統內部設定檔與本地端資料庫存放區
│       ├── accounting.db                # SQLite 實體關聯式資料庫檔案
│       ├── config.json                  # 使用者設定檔 (如記住登入狀態等)
│       └── qr_宏.png                    # 系統自動生成的個人 QR Code 圖片
├── doc                                   # 專案文件與資料庫結構紀錄 (存放 .sql)
│   └── schema.sql                       # 資料庫資料表結構定義檔 (DDL)
├── frontend                              # 前端使用者介面層
│   └── ui                               # 使用者介面 (UI) 主要目錄，負責所有視覺顯示
│       ├── AccountingGUI.py             # 主程式 GUI 視窗入口，整合各頁面
│       ├── components                   # 共用的 UI 元件與彈跳對話框
│       │   ├── common.py               # 通用介面框架 (如登入畫面 LoginFrame)
│       │   └── dialogs.py              # 所有自訂對話框 (群組建立、交易新增等)
│       ├── group                        # 群組記帳與功能相關的介面
│       │   └── group_frame.py          # 群組詳細分帳、成員管理的操作畫面
│       └── personal                     # 個人記帳與互動相關的介面
│           ├── friends_frame.py         # 朋友清單與加入好友互動畫面
│           └── personal_frame.py        # 個人流水帳、掃描或顯示 QR Code 的畫面
├── requirements.txt                      # Python 套件依賴列表 (建議保留)
├── run.py                                # 應用程式快速啟動入口 (主程式啟動腳本)
├── upload_changes.py                     # 自動化 Git 上傳變更腳本 (Python)
└── 工具                                  # 開發、部署與維護專用的輔助文件與腳本
    ├── run.bat                           # 一鍵快速啟動主程式的批次檔 (供非開發者使用)
    ├── sync_latest.bat                   # 一鍵同步遠端最新程式碼的批次檔
    ├── upload_changes.bat                # 一鍵自動將本地端修改上傳的批次檔
    ├── 前置作業SOP.md                    # 開始開發前需進行的標準工作指引
    ├── 完成後作業SOP.md                  # 完成開發後應進行的標準工作指引
    ├── 計劃書.txt                        # 系統開發初期的企劃與需求紀錄
    └── 資料夾架構.md                     # 紀錄當前專案完整的目錄與檔案說明
```
