```text
group ledger/
├── .gitignore                            # Git 忽略規則
├── backend                               # 後端業務邏輯與資料層
│   ├── core                             # 核心邏輯目錄，負責所有資料處理與邏輯運算
│   │   ├── base.py                     # 基礎服務類別，負責資料庫連線初始化與共用設定
│   │   ├── db_update.py                # 資料庫更新與維護腳本
│   │   ├── group_service.py            # 群組記帳與邏輯服務 (支援雙模式結算)
│   │   ├── main.py                     # 系統核心邏輯入口 (含動態逾期檢查與排程)
│   │   ├── models.py                   # 各資料模型 (Models) 與狀態列舉 (Enum)
│   │   └── personal_service.py         # 個人記帳 (含私帳)、好友關係與 QR Code 處理
│   └── data                             # 系統內部設定檔與本地端資料庫存放區
│       ├── accounting.db                # SQLite 實體關聯式資料庫檔案
│       └── config.json                  # 使用者設定檔 (如記住登入狀態等)
├── doc                                   # 專案文件與資料庫結構紀錄 (存放 .sql)
│   ├── migrations.sql                   # 資料庫遷移與變更紀錄 SQL
│   └── schema.sql                       # 資料庫資料表結構定義檔 (DDL)
├── frontend                              # 前端使用者介面層
│   └── ui                               # 使用者介面 (UI) 主要目錄，負責所有視覺顯示
│       ├── AccountingGUI.py             # 主程式 GUI 入口，整合分頁與「全局記帳」按鍵
│       ├── analysis                     # 數據分析與視覺化分頁工具
│       │   ├── analysis_frame.py       # 提供支出圓餅圖與墊付頻率長條圖 (Matplotlib)
│       │   └── calendar_frame.py       # 按日期查看交易明細的日曆視圖 (tkcalendar)
│       ├── components                   # 共用的 UI 元件與彈跳對對話框
│       │   ├── common.py               # 通用介面框架 (如登入畫面 LoginFrame)
│       │   └── dialogs.py              # 所有自訂對話框 (群組建立、交易新增等)
│       ├── group                        # 群組記帳與功能相關的介面
│       │   └── group_frame.py          # 群組管理、成員檢視與雙模式結算操作畫面
│       └── personal                     # 個人記帳與互動相關的介面
│           ├── friends_frame.py         # 朋友清單與加入好友互動畫面
│           └── personal_frame.py        # 個人帳單摘要與「個人支出流水」歷史畫面
├── requirements.txt                      # Python 套件列表 (包含 matplotlib, tkcalendar, schedule)
├── run.py                                # 應用程式快速啟動入口
├── upload_changes.py                     # 自動化 Git 上傳變更腳本
└── 工具                                  # 開發輔助文件與腳本
    ├── run.bat                           # 一鍵快速啟動主程式
    ├── sync_latest.bat                   # 一鍵同步遠端最新程式碼
    ├── update_db.bat                     # 一鍵執行資料庫更新腳本
    ├── upload_changes.bat                # 一鍵自動將本地端修改上傳
    ├── 前置作業SOP.md                    # 開始開發前需進行的標準工作指引
    ├── 完成後作業SOP.md                  # 完成開發後應進行的標準工作指引
    ├── 情境敘述.txt                        # 專案測試情境與資料預設值
    ├── 深入技術報告.md                     # 詳細的系統架構與演算法說明報告
    ├── 系統介紹.md                        # 專案功能概要與商業價值說明
    ├── 計劃書.md                          # 最新的專案計畫與設計規範
    └── 資料夾架構.md                       # 紀錄當前專案完整的目錄與檔案說明 (本文檔)
```
